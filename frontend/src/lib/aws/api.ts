// Minimal placeholder — API Gateway calls use Bearer JWT (not SigV4).
// apiRequest is implemented in mikeApi.ts using Cognito session tokens.
// This file is kept for future use if SigV4 signing is needed for other AWS services.

export {};
